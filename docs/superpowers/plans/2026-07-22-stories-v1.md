# Сторисы v1 — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Дать жайдемчилер публиковать фото-сторисы с автоистечением через 24 часа и смотреть чужие через ленту в верхней части страницы Жайдемчилер.

**Architecture:** Хранение в Django REST (модель `Story`, эндпоинт `/core/story/`), истечение — фильтр на чтении без крона. Фото грузится через существующий `/category/image/`, в базе лежит URL. Отметка «просмотрено» — локально в `SharedPreferences`.

**Tech Stack:** Django 5.2 + DRF (`jaidem_backend`); Flutter + flutter_bloc + GetIt + auto_route + Dio (`jaidem_student`).

**Спек:** `jaidem_student/docs/superpowers/specs/2026-07-22-stories-design.md`

## Global Constraints

- Два репозитория. Бэкенд: `/Users/kyrgyzbala/projects/jaidem/jaidem_backend`. Приложение: `/Users/kyrgyzbala/projects/jaidem/jaidem_student`. Команды выполнять **внутри** соответствующей папки; общего тулинга в корне нет.
- Время жизни сториса: **24 часа**, константа `STORY_TTL_HOURS = 24` объявляется один раз в `core/models.py`.
- Эндпоинт ленты **обязан** иметь `pagination_class = None`: в `jaidem/settings.py` глобально включён `PageNumberPagination` с `PAGE_SIZE: 30`, он обернёт сгруппированный ответ и сломает ленту.
- Автор сториса берётся **только** из `request.user`, никогда из тела запроса.
- Новые строки UI добавляются **во все три** языковые мапы (`ky`, `ru`, `en`) в `lib/core/localization/app_localizations.dart`. Локализация здесь — ручные Dart-мапы, ARB не используется.
- Брендовые цвета: primary `#3C2A98`, secondary `#FF854A` (`AppColors.primary`, `AppColors.secondary`).
- Новый эндпоинт загрузки картинок **не создаётся** — переиспользуется `POST /category/image/`.
- Никаких `Co-Authored-By` и прочих трейлеров в коммитах. `.claude/` в git не добавлять.

---

## Структура файлов

**Бэкенд (`jaidem_backend`)**

| Файл | Ответственность |
|---|---|
| `core/models.py` (изменить) | Константа `STORY_TTL_HOURS`, модель `Story` |
| `core/migrations/0035_story.py` (создать через makemigrations) | Таблица `Story` |
| `core/serializers.py` (изменить) | `StorySerializer`, `StoryAuthorSerializer` |
| `core/views.py` (изменить) | `StoryViewSet`: лента, создание, удаление |
| `core/urls.py` (изменить) | Регистрация роутера `story` |
| `core/admin.py` (изменить) | `StoryAdmin` — инструмент модерации |
| `core/tests.py` (изменить) | 3 теста: TTL, автор, чужое удаление |

**Приложение (`jaidem_student`)**

| Файл | Ответственность |
|---|---|
| `lib/core/utils/constants/api_const.dart` (изменить) | `stories`, `imageUpload` |
| `lib/core/localization/app_localizations.dart` (изменить) | 7 ключей × 3 языка |
| `lib/features/profile/presentation/pages/profile_edit_form_page.dart` (изменить) | Убрать захардкоженный URL |
| `lib/features/stories/data/models/story_model.dart` | `StoryModel`, `StoryAuthorModel`, `StoryGroupModel` |
| `lib/features/stories/domain/story_ordering.dart` | Чистая функция сортировки ленты |
| `lib/features/stories/data/datasources/seen_stories_store.dart` | Локальная отметка «просмотрено» |
| `lib/features/stories/data/datasources/stories_remote_data_source.dart` (+ `_impl`) | HTTP-слой |
| `lib/features/stories/domain/usecases/stories_usecase.dart` | Обёртка над датасорсом |
| `lib/features/stories/presentation/cubit/stories_cubit.dart` (+ `stories_state.dart`) | Состояние ленты |
| `lib/features/stories/presentation/widgets/stories_strip.dart` | Горизонтальная лента аватарок |
| `lib/features/stories/presentation/pages/story_viewer_page.dart` | Полноэкранный просмотрщик |
| `lib/features/stories/stories_injection.dart` | Регистрация в GetIt |
| `lib/core/data/injection.dart` (изменить) | Вызов `storiesInjection()` |
| `lib/core/app/app.dart` (изменить) | `BlocProvider<StoriesCubit>` |
| `lib/core/routes/app_router.dart` (изменить) | `StoryViewerRoute` |
| `lib/features/jaidems/presentation/pages/jaidems_page.dart` (изменить) | Монтаж ленты в `headerSliverBuilder` |
| `test/features/stories/*_test.dart` | Юнит-тесты чистой логики |

**Отклонение от спека (осознанное).** Спек описывал слой репозиториев (`domain/repositories` + `data/repositories`). Соседняя фича `jaidems`, рядом с которой живут сторисы, его **не** использует: `datasource → usecase → cubit`. Для трёх операций репозиторий был бы пустым проксированием, поэтому следуем паттерну `jaidems`. Функциональность спека не меняется.

---

### Task 1: Бэкенд — модель `Story`, миграция, админка

**Files:**
- Modify: `jaidem_backend/core/models.py`
- Create: `jaidem_backend/core/migrations/0035_story.py` (генерируется командой; последняя существующая — `0034_flow_datecreated`)
- Modify: `jaidem_backend/core/admin.py`

**Interfaces:**
- Consumes: `Jaidemchiler` из `user.models` (уже импортирован в `core/models.py`)
- Produces: `core.models.Story` (поля `id`, `author`, `photo`, `created_at`), константа `core.models.STORY_TTL_HOURS`

- [ ] **Step 1: Добавить константу и модель в `core/models.py`**

Добавить в конец файла:

```python
STORY_TTL_HOURS = 24


class Story(models.Model):
    class Meta:
        verbose_name = 'Сторис'
        verbose_name_plural = 'Сторисы'
        ordering = ['created_at']

    author = models.ForeignKey(
        Jaidemchiler,
        on_delete=models.CASCADE,
        related_name='stories',
        verbose_name='Автор',
    )
    photo = models.TextField('Фото', help_text='Ссылка на изображение')
    created_at = models.DateTimeField('Дата создания', auto_now_add=True)

    def __str__(self):
        return f'{self.author} — {self.created_at:%d.%m.%Y %H:%M}'
```

`CASCADE` (а не `SET_NULL`, как у `ForumPost`) — лента группируется по автору, сторис без автора отрисовать негде.

- [ ] **Step 2: Проверить, что миграция затронет только `Story`**

Run: `python manage.py makemigrations core --name story --dry-run --verbosity 2`

Expected: в выводе **только** `Create model Story`.

Если появились другие изменения — модели `core` разошлись с миграциями. Остановиться и сообщить об этом: сгребать чужой дрейф в свою миграцию нельзя.

- [ ] **Step 3: Создать миграцию**

Run: `python manage.py makemigrations core --name story`
Expected: `Migrations for 'core': core/migrations/0035_story.py + Create model Story`

- [ ] **Step 4: Применить миграцию**

Run: `python manage.py migrate core`
Expected: `Applying core.0035_story... OK`

- [ ] **Step 5: Зарегистрировать модель в админке**

В `core/admin.py` добавить в конец (стиль совпадает с остальными — декоратор `@admin.register`):

```python
@admin.register(models.Story)
class StoryAdmin(admin.ModelAdmin):
    list_display = ('id', 'author', 'created_at')
    list_filter = ('created_at',)
    search_fields = ('author__fullname', 'author__login')
    readonly_fields = ('created_at',)
    autocomplete_fields = ('author',)
```

- [ ] **Step 6: Проверить конфигурацию**

Run: `python manage.py check`
Expected: `System check identified no issues (0 silenced).`

Если ругается на `autocomplete_fields = ('author',)` — значит у `JaidemchilerAdmin` нет `search_fields`. В этом случае убрать строку `autocomplete_fields` и перезапустить проверку.

- [ ] **Step 7: Коммит**

```bash
cd /Users/kyrgyzbala/projects/jaidem/jaidem_backend
git add core/models.py core/admin.py core/migrations/
git commit -m "feat(stories): добавить модель Story, миграцию и админку"
```

---

### Task 2: Бэкенд — API ленты, создания и удаления

**Files:**
- Modify: `jaidem_backend/core/tests.py`
- Modify: `jaidem_backend/core/serializers.py`
- Modify: `jaidem_backend/core/views.py`
- Modify: `jaidem_backend/core/urls.py`

**Interfaces:**
- Consumes: `core.models.Story`, `core.models.STORY_TTL_HOURS` (Task 1)
- Produces: HTTP API
  - `GET /jaidem/api/core/story/` → `[{author: {id, fullname, avatar, isAdvisor}, stories: [{id, photo, created_at}]}]`
  - `POST /jaidem/api/core/story/` body `{photo: str}` → `{id, photo, created_at}`, 201
  - `DELETE /jaidem/api/core/story/{id}/` → 204, чужой → 404

- [ ] **Step 1: Написать падающие тесты**

Заменить содержимое `core/tests.py` на:

```python
from datetime import timedelta

from django.utils import timezone
from rest_framework import status
from rest_framework.test import APITestCase

from core.models import Story, STORY_TTL_HOURS
from user.models import Jaidemchiler

STORY_URL = '/jaidem/api/core/story/'


class StoryApiTests(APITestCase):
    def setUp(self):
        self.author = Jaidemchiler.objects.create(
            login='author_one', fullname='Author One', user_type='student',
        )
        self.other = Jaidemchiler.objects.create(
            login='author_two', fullname='Author Two', user_type='student',
        )

    def test_feed_excludes_expired_stories(self):
        fresh = Story.objects.create(author=self.author, photo='https://example.com/fresh.jpg')
        stale = Story.objects.create(author=self.author, photo='https://example.com/stale.jpg')
        Story.objects.filter(pk=stale.pk).update(
            created_at=timezone.now() - timedelta(hours=STORY_TTL_HOURS + 1)
        )

        self.client.force_authenticate(user=self.author)
        response = self.client.get(STORY_URL)

        self.assertEqual(response.status_code, status.HTTP_200_OK)
        self.assertEqual(len(response.data), 1)
        self.assertEqual([s['id'] for s in response.data[0]['stories']], [fresh.id])

    def test_create_uses_request_user_as_author(self):
        self.client.force_authenticate(user=self.author)
        response = self.client.post(
            STORY_URL,
            {'photo': 'https://example.com/new.jpg', 'author': self.other.pk},
            format='json',
        )

        self.assertEqual(response.status_code, status.HTTP_201_CREATED)
        story = Story.objects.get(pk=response.data['id'])
        self.assertEqual(story.author_id, self.author.pk)

    def test_cannot_delete_other_users_story(self):
        story = Story.objects.create(author=self.other, photo='https://example.com/other.jpg')

        self.client.force_authenticate(user=self.author)
        response = self.client.delete(f'{STORY_URL}{story.pk}/')

        self.assertEqual(response.status_code, status.HTTP_404_NOT_FOUND)
        self.assertTrue(Story.objects.filter(pk=story.pk).exists())
```

`Story.objects.filter(...).update(...)` обходит `auto_now_add` — только так можно состарить запись.

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `python manage.py test core.tests.StoryApiTests -v 2`
Expected: FAIL — все три теста, ответ `404` (роут ещё не зарегистрирован).

- [ ] **Step 3: Добавить сериализаторы**

В `core/serializers.py` добавить в конец (`models` и `Jaidemchiler` уже импортированы):

```python
class StoryAuthorSerializer(serializers.ModelSerializer):
    class Meta:
        model = Jaidemchiler
        fields = ('id', 'fullname', 'avatar', 'isAdvisor')


class StorySerializer(serializers.ModelSerializer):
    class Meta:
        model = models.Story
        fields = ('id', 'photo', 'created_at')
        read_only_fields = ('created_at',)
```

`author` намеренно отсутствует в `fields` — подменить автора через тело запроса невозможно.

- [ ] **Step 4: Добавить вьюсет**

В `core/views.py` добавить в конец. Все нужные импорты в файле уже есть: `timezone` (стр. 2), `viewsets/permissions/status` (стр. 3), `Response` (стр. 5), `timedelta` (стр. 7), `defaultdict` (стр. 9), `Jaidemchiler` (стр. 15).

```python
class StoryViewSet(viewsets.ModelViewSet):
    """Сторисы: лента активных за 24ч, создание и удаление своих."""

    permission_classes = [permissions.IsAuthenticated]
    serializer_class = serializers.StorySerializer
    queryset = models.Story.objects.all()
    # Глобальная пагинация обернула бы сгруппированный ответ и разбила ленту постранично.
    pagination_class = None

    @staticmethod
    def active_stories():
        cutoff = timezone.now() - timedelta(hours=models.STORY_TTL_HOURS)
        return models.Story.objects.filter(created_at__gt=cutoff).select_related('author')

    def get_queryset(self):
        if self.action in ('destroy', 'update', 'partial_update'):
            # Изменять и удалять можно только свои сторисы — чужой вернёт 404.
            return models.Story.objects.filter(author_id=self.request.user.pk)
        return self.active_stories()

    def list(self, request, *args, **kwargs):
        grouped = defaultdict(list)
        for story in self.active_stories().order_by('author_id', 'created_at'):
            grouped[story.author_id].append(story)

        payload = [
            {
                'author': serializers.StoryAuthorSerializer(items[0].author).data,
                'stories': serializers.StorySerializer(items, many=True).data,
            }
            for items in grouped.values()
        ]
        # Авторы со свежими сторисами — выше.
        payload.sort(key=lambda group: group['stories'][-1]['created_at'], reverse=True)
        return Response(payload)

    def create(self, request, *args, **kwargs):
        if not Jaidemchiler.objects.filter(pk=request.user.pk).exists():
            return Response(
                {'detail': 'Публиковать сторисы могут только жайдемчилер.'},
                status=status.HTTP_403_FORBIDDEN,
            )
        return super().create(request, *args, **kwargs)

    def perform_create(self, serializer):
        serializer.save(author_id=self.request.user.pk)
```

- [ ] **Step 5: Зарегистрировать роут**

В `core/urls.py` добавить строку в блок регистраций роутера, после `router.register(r'project_interested', views.ProjectInterestedViewSet)`:

```python
router.register(r'story', views.StoryViewSet)
```

- [ ] **Step 6: Убедиться, что тесты проходят**

Run: `python manage.py test core.tests.StoryApiTests -v 2`
Expected: `Ran 3 tests ... OK`

- [ ] **Step 7: Коммит**

```bash
cd /Users/kyrgyzbala/projects/jaidem/jaidem_backend
git add core/serializers.py core/views.py core/urls.py core/tests.py
git commit -m "feat(stories): API ленты, создания и удаления сторисов"
```

---

### Task 3: Приложение — константы, локализация, вынос захардкоженного URL

**Files:**
- Modify: `jaidem_student/lib/core/utils/constants/api_const.dart`
- Modify: `jaidem_student/lib/core/localization/app_localizations.dart`
- Modify: `jaidem_student/lib/features/profile/presentation/pages/profile_edit_form_page.dart`

**Interfaces:**
- Produces: `ApiConst.stories`, `ApiConst.imageUpload`; ключи локализации `stories_*`

- [ ] **Step 1: Добавить константы эндпоинтов**

В `lib/core/utils/constants/api_const.dart`, сразу после блока `// Profile`:

```dart
  // Stories
  static const String stories = '/core/story/';

  // Загрузка изображений (multipart). Используется сторисами и аватаром профиля.
  static const String imageUpload = '/category/image/';
```

- [ ] **Step 2: Добавить ключи локализации во все три мапы**

В `lib/core/localization/app_localizations.dart` добавить блок в **каждую** из трёх мап.

В кыргызскую (`_kyStrings`):

```dart
    // Stories
    'stories_your_story': 'Сенин сторисиң',
    'stories_add': 'Сторис кошуу',
    'stories_delete': 'Өчүрүү',
    'stories_delete_confirm': 'Бул стористи өчүрөсүзбү?',
    'stories_deleted': 'Сторис өчүрүлдү',
    'stories_upload_failed': 'Сүрөт жүктөлгөн жок',
    'stories_create_failed': 'Сторис түзүлгөн жок',
```

В русскую (`_ruStrings`):

```dart
    // Stories
    'stories_your_story': 'Твоя история',
    'stories_add': 'Добавить историю',
    'stories_delete': 'Удалить',
    'stories_delete_confirm': 'Удалить эту историю?',
    'stories_deleted': 'История удалена',
    'stories_upload_failed': 'Не удалось загрузить фото',
    'stories_create_failed': 'Не удалось создать историю',
```

В английскую (`_enStrings`):

```dart
    // Stories
    'stories_your_story': 'Your story',
    'stories_add': 'Add story',
    'stories_delete': 'Delete',
    'stories_delete_confirm': 'Delete this story?',
    'stories_deleted': 'Story deleted',
    'stories_upload_failed': 'Failed to upload photo',
    'stories_create_failed': 'Failed to create story',
```

- [ ] **Step 3: Убрать захардкоженный URL в профиле**

В `lib/features/profile/presentation/pages/profile_edit_form_page.dart` заменить:

```dart
      final response = await dio.post(
        'https://jaidem-back.ru/jaidem/api/category/image/',
        data: formData,
      );
```

на:

```dart
      final response = await dio.post(
        ApiConst.imageUpload,
        data: formData,
      );
```

Добавить импорт, если его нет:

```dart
import 'package:jaidem/core/utils/constants/api_const.dart';
```

У `DioNetwork.appAPI` уже задан `baseUrl`, поэтому относительный путь резолвится в тот же адрес.

- [ ] **Step 4: Проверить анализатором**

Run: `flutter analyze lib/core/utils/constants/api_const.dart lib/core/localization/app_localizations.dart lib/features/profile/presentation/pages/profile_edit_form_page.dart`
Expected: `No issues found!`

- [ ] **Step 5: Коммит**

```bash
cd /Users/kyrgyzbala/projects/jaidem/jaidem_student
git add lib/core/utils/constants/api_const.dart lib/core/localization/app_localizations.dart lib/features/profile/presentation/pages/profile_edit_form_page.dart
git commit -m "feat(stories): константы эндпоинтов и строки локализации"
```

---

### Task 4: Приложение — модели сториса

**Files:**
- Create: `jaidem_student/lib/features/stories/data/models/story_model.dart`
- Create: `jaidem_student/test/features/stories/story_model_test.dart`

**Interfaces:**
- Produces:
  - `StoryModel({required int id, required String photo, required DateTime createdAt})`, `StoryModel.fromJson(Map<String, dynamic>)`
  - `StoryAuthorModel({required int id, String? fullname, String? avatar, required bool isAdvisor})`, `.fromJson`
  - `StoryGroupModel({required StoryAuthorModel author, required List<StoryModel> stories})`, `.fromJson`

- [ ] **Step 1: Написать падающий тест**

Создать `test/features/stories/story_model_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:jaidem/features/stories/data/models/story_model.dart';

void main() {
  test('StoryGroupModel.fromJson разбирает автора и список сторисов', () {
    final json = {
      'author': {
        'id': 12,
        'fullname': 'Айбек',
        'avatar': 'https://example.com/a.jpg',
        'isAdvisor': true,
      },
      'stories': [
        {'id': 340, 'photo': 'https://example.com/1.jpg', 'created_at': '2026-07-22T09:14:00Z'},
        {'id': 341, 'photo': 'https://example.com/2.jpg', 'created_at': '2026-07-22T10:02:00Z'},
      ],
    };

    final group = StoryGroupModel.fromJson(json);

    expect(group.author.id, 12);
    expect(group.author.fullname, 'Айбек');
    expect(group.author.isAdvisor, isTrue);
    expect(group.stories.map((s) => s.id).toList(), [340, 341]);
    expect(group.stories.first.photo, 'https://example.com/1.jpg');
    expect(group.stories.last.createdAt.toUtc().hour, 10);
  });

  test('StoryAuthorModel.fromJson переживает отсутствующие поля', () {
    final author = StoryAuthorModel.fromJson({'id': 5});

    expect(author.id, 5);
    expect(author.fullname, isNull);
    expect(author.avatar, isNull);
    expect(author.isAdvisor, isFalse);
  });
}
```

- [ ] **Step 2: Запустить тест — должен упасть**

Run: `flutter test test/features/stories/story_model_test.dart`
Expected: FAIL — `Target of URI doesn't exist: '.../story_model.dart'`

- [ ] **Step 3: Написать модели**

Создать `lib/features/stories/data/models/story_model.dart`:

```dart
class StoryModel {
  final int id;
  final String photo;
  final DateTime createdAt;

  const StoryModel({
    required this.id,
    required this.photo,
    required this.createdAt,
  });

  factory StoryModel.fromJson(Map<String, dynamic> json) {
    return StoryModel(
      id: json['id'] as int,
      photo: (json['photo'] as String?) ?? '',
      createdAt:
          DateTime.tryParse(json['created_at'] as String? ?? '') ?? DateTime.now(),
    );
  }
}

class StoryAuthorModel {
  final int id;
  final String? fullname;
  final String? avatar;
  final bool isAdvisor;

  const StoryAuthorModel({
    required this.id,
    this.fullname,
    this.avatar,
    required this.isAdvisor,
  });

  factory StoryAuthorModel.fromJson(Map<String, dynamic> json) {
    return StoryAuthorModel(
      id: json['id'] as int,
      fullname: json['fullname'] as String?,
      avatar: json['avatar'] as String?,
      isAdvisor: (json['isAdvisor'] as bool?) ?? false,
    );
  }
}

class StoryGroupModel {
  final StoryAuthorModel author;
  final List<StoryModel> stories;

  const StoryGroupModel({required this.author, required this.stories});

  factory StoryGroupModel.fromJson(Map<String, dynamic> json) {
    return StoryGroupModel(
      author: StoryAuthorModel.fromJson(json['author'] as Map<String, dynamic>),
      stories: ((json['stories'] as List<dynamic>?) ?? const [])
          .map((e) => StoryModel.fromJson(e as Map<String, dynamic>))
          .toList(),
    );
  }
}
```

- [ ] **Step 4: Запустить тест — должен пройти**

Run: `flutter test test/features/stories/story_model_test.dart`
Expected: `All tests passed!`

- [ ] **Step 5: Коммит**

```bash
cd /Users/kyrgyzbala/projects/jaidem/jaidem_student
git add lib/features/stories/data/models/story_model.dart test/features/stories/story_model_test.dart
git commit -m "feat(stories): модели сториса, автора и группы"
```

---

### Task 5: Приложение — отметка «просмотрено» и сортировка ленты

Вся нетривиальная клиентская логика собрана здесь, потому что она чистая и проверяется юнит-тестами без виджетов.

**Files:**
- Create: `jaidem_student/lib/features/stories/data/datasources/seen_stories_store.dart`
- Create: `jaidem_student/lib/features/stories/domain/story_ordering.dart`
- Create: `jaidem_student/test/features/stories/seen_stories_store_test.dart`
- Create: `jaidem_student/test/features/stories/story_ordering_test.dart`

**Interfaces:**
- Consumes: `StoryGroupModel`, `StoryModel`, `StoryAuthorModel` (Task 4)
- Produces:
  - `SeenStoriesStore(SharedPreferences prefs)` с `Set<int> read()`, `Future<void> markSeen(int)`, `Future<void> pruneTo(Set<int>)`
  - `List<StoryGroupModel> sortStoryGroups({required List<StoryGroupModel> groups, required Set<int> seenStoryIds, required int? currentUserId})`

- [ ] **Step 1: Написать падающие тесты**

Создать `test/features/stories/seen_stories_store_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:jaidem/features/stories/data/datasources/seen_stories_store.dart';
import 'package:shared_preferences/shared_preferences.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  late SharedPreferences prefs;

  setUp(() async {
    SharedPreferences.setMockInitialValues({});
    prefs = await SharedPreferences.getInstance();
  });

  test('markSeen сохраняет id, read его возвращает', () async {
    final store = SeenStoriesStore(prefs);

    await store.markSeen(10);
    await store.markSeen(11);

    expect(store.read(), {10, 11});
  });

  test('markSeen не создаёт дублей', () async {
    final store = SeenStoriesStore(prefs);

    await store.markSeen(10);
    await store.markSeen(10);

    expect(store.read(), {10});
  });

  test('pruneTo оставляет только активные id', () async {
    final store = SeenStoriesStore(prefs);
    await store.markSeen(10);
    await store.markSeen(11);
    await store.markSeen(12);

    await store.pruneTo({11, 12, 99});

    expect(store.read(), {11, 12});
  });
}
```

Создать `test/features/stories/story_ordering_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:jaidem/features/stories/data/models/story_model.dart';
import 'package:jaidem/features/stories/domain/story_ordering.dart';

StoryGroupModel _group(int authorId, List<int> storyIds) {
  return StoryGroupModel(
    author: StoryAuthorModel(id: authorId, isAdvisor: false),
    stories: storyIds
        .map((id) => StoryModel(id: id, photo: '', createdAt: DateTime(2026, 7, 22)))
        .toList(),
  );
}

void main() {
  test('свои идут первыми, затем непросмотренные, затем просмотренные', () {
    final groups = [
      _group(1, [100]), // просмотрен целиком
      _group(2, [200]), // непросмотрен
      _group(3, [300]), // свой
    ];

    final result = sortStoryGroups(
      groups: groups,
      seenStoryIds: {100},
      currentUserId: 3,
    );

    expect(result.map((g) => g.author.id).toList(), [3, 2, 1]);
  });

  test('группа просмотрена, только если просмотрены все её сторисы', () {
    final groups = [
      _group(1, [100, 101]), // просмотрен лишь частично → считается непросмотренным
      _group(2, [200, 201]), // просмотрен полностью
    ];

    final result = sortStoryGroups(
      groups: groups,
      seenStoryIds: {100, 200, 201},
      currentUserId: 999,
    );

    expect(result.map((g) => g.author.id).toList(), [1, 2]);
  });

  test('внутри корзины сохраняется исходный порядок', () {
    final groups = [_group(1, [100]), _group(2, [200]), _group(3, [300])];

    final result = sortStoryGroups(
      groups: groups,
      seenStoryIds: const {},
      currentUserId: null,
    );

    expect(result.map((g) => g.author.id).toList(), [1, 2, 3]);
  });
}
```

- [ ] **Step 2: Запустить тесты — должны упасть**

Run: `flutter test test/features/stories/`
Expected: FAIL — `Target of URI doesn't exist` для `seen_stories_store.dart` и `story_ordering.dart`.

- [ ] **Step 3: Реализовать `SeenStoriesStore`**

Создать `lib/features/stories/data/datasources/seen_stories_store.dart`:

```dart
import 'package:shared_preferences/shared_preferences.dart';

/// Локальная отметка «этот сторис уже просмотрен».
///
/// Хранится только на устройстве: счётчик «кто посмотрел» в v1 не делаем,
/// поэтому на сервер эта информация не уходит.
class SeenStoriesStore {
  static const String _key = 'seen_story_ids';

  final SharedPreferences prefs;

  const SeenStoriesStore(this.prefs);

  Set<int> read() {
    final raw = prefs.getStringList(_key) ?? const <String>[];
    return raw.map(int.tryParse).whereType<int>().toSet();
  }

  Future<void> markSeen(int storyId) => _write(read()..add(storyId));

  /// Оставляет только ещё активные id, чтобы набор не рос бесконечно.
  Future<void> pruneTo(Set<int> activeStoryIds) =>
      _write(read().intersection(activeStoryIds));

  Future<void> _write(Set<int> ids) =>
      prefs.setStringList(_key, ids.map((e) => e.toString()).toList());
}
```

- [ ] **Step 4: Реализовать сортировку**

Создать `lib/features/stories/domain/story_ordering.dart`:

```dart
import 'package:jaidem/features/stories/data/models/story_model.dart';

/// Раскладывает ленту по трём корзинам: свои → непросмотренные → просмотренные.
///
/// Сервер отдаёт группы по свежести и ничего не знает про «просмотрено»
/// (оно локальное), поэтому финальный порядок задаётся здесь. Внутри каждой
/// корзины исходный порядок по свежести сохраняется.
List<StoryGroupModel> sortStoryGroups({
  required List<StoryGroupModel> groups,
  required Set<int> seenStoryIds,
  required int? currentUserId,
}) {
  final own = <StoryGroupModel>[];
  final unseen = <StoryGroupModel>[];
  final seen = <StoryGroupModel>[];

  for (final group in groups) {
    if (currentUserId != null && group.author.id == currentUserId) {
      own.add(group);
    } else if (group.stories.every((s) => seenStoryIds.contains(s.id))) {
      // Группа считается просмотренной, только если просмотрены все её сторисы.
      seen.add(group);
    } else {
      unseen.add(group);
    }
  }

  return [...own, ...unseen, ...seen];
}
```

- [ ] **Step 5: Запустить тесты — должны пройти**

Run: `flutter test test/features/stories/`
Expected: `All tests passed!` (8 тестов вместе с Task 4)

- [ ] **Step 6: Коммит**

```bash
cd /Users/kyrgyzbala/projects/jaidem/jaidem_student
git add lib/features/stories/data/datasources/seen_stories_store.dart lib/features/stories/domain/story_ordering.dart test/features/stories/
git commit -m "feat(stories): локальная отметка просмотра и сортировка ленты"
```

---

### Task 6: Приложение — датасорс, usecase, кубит и регистрация в DI

**Files:**
- Create: `jaidem_student/lib/features/stories/data/datasources/stories_remote_data_source.dart`
- Create: `jaidem_student/lib/features/stories/data/datasources/stories_remote_data_source_impl.dart`
- Create: `jaidem_student/lib/features/stories/domain/usecases/stories_usecase.dart`
- Create: `jaidem_student/lib/features/stories/presentation/cubit/stories_cubit.dart`
- Create: `jaidem_student/lib/features/stories/presentation/cubit/stories_state.dart`
- Create: `jaidem_student/lib/features/stories/stories_injection.dart`
- Modify: `jaidem_student/lib/core/data/injection.dart`
- Modify: `jaidem_student/lib/core/app/app.dart`

**Interfaces:**
- Consumes: `ApiConst.stories`, `ApiConst.imageUpload` (Task 3); модели (Task 4); `SeenStoriesStore`, `sortStoryGroups` (Task 5)
- Produces: `StoriesCubit` с `fetchFeed()`, `Future<bool> createStory(File)`, `Future<bool> deleteStory(int)`, `Future<void> markSeen(int)`; состояния `StoriesInitial/Loading/Loaded/Error`; `storiesInjection()`

- [ ] **Step 1: Описать контракт датасорса**

Создать `lib/features/stories/data/datasources/stories_remote_data_source.dart`:

```dart
import 'dart:io';

import 'package:dartz/dartz.dart';
import 'package:jaidem/features/stories/data/models/story_model.dart';

abstract class StoriesRemoteDataSource {
  Future<Either<String, List<StoryGroupModel>>> getStories();

  /// Загружает файл и возвращает URL картинки.
  Future<Either<String, String>> uploadPhoto(File file);

  Future<Either<String, StoryModel>> createStory(String photoUrl);

  Future<Either<String, Unit>> deleteStory(int storyId);
}
```

- [ ] **Step 2: Реализовать датасорс**

Создать `lib/features/stories/data/datasources/stories_remote_data_source_impl.dart`:

```dart
import 'dart:io';

import 'package:dartz/dartz.dart';
import 'package:dio/dio.dart';
import 'package:jaidem/core/utils/constants/api_const.dart';
import 'package:jaidem/features/stories/data/datasources/stories_remote_data_source.dart';
import 'package:jaidem/features/stories/data/models/story_model.dart';

class StoriesRemoteDataSourceImpl implements StoriesRemoteDataSource {
  final Dio dio;

  const StoriesRemoteDataSourceImpl({required this.dio});

  @override
  Future<Either<String, List<StoryGroupModel>>> getStories() async {
    try {
      final response = await dio.get(ApiConst.stories);
      final data = (response.data as List<dynamic>)
          .map((e) => StoryGroupModel.fromJson(e as Map<String, dynamic>))
          .toList();
      return Right(data);
    } catch (e) {
      return Left('Failed to load stories: $e');
    }
  }

  @override
  Future<Either<String, String>> uploadPhoto(File file) async {
    try {
      final formData = FormData.fromMap({
        'title': 'story_${DateTime.now().millisecondsSinceEpoch}',
        'image': await MultipartFile.fromFile(file.path, filename: 'story.jpg'),
      });

      final response = await dio.post(ApiConst.imageUpload, data: formData);
      final url = response.data['image'] as String?;
      if (url == null || url.isEmpty) {
        return const Left('Upload returned no image url');
      }
      return Right(url);
    } catch (e) {
      return Left('Failed to upload photo: $e');
    }
  }

  @override
  Future<Either<String, StoryModel>> createStory(String photoUrl) async {
    try {
      final response = await dio.post(ApiConst.stories, data: {'photo': photoUrl});
      return Right(StoryModel.fromJson(response.data as Map<String, dynamic>));
    } catch (e) {
      return Left('Failed to create story: $e');
    }
  }

  @override
  Future<Either<String, Unit>> deleteStory(int storyId) async {
    try {
      await dio.delete('${ApiConst.stories}$storyId/');
      return const Right(unit);
    } catch (e) {
      return Left('Failed to delete story: $e');
    }
  }
}
```

- [ ] **Step 3: Написать usecase**

Создать `lib/features/stories/domain/usecases/stories_usecase.dart` (по образцу `GetJaidemsUsecase`):

```dart
import 'dart:io';

import 'package:dartz/dartz.dart';
import 'package:jaidem/features/stories/data/datasources/stories_remote_data_source.dart';
import 'package:jaidem/features/stories/data/models/story_model.dart';

class StoriesUsecase {
  final StoriesRemoteDataSource repository;

  const StoriesUsecase(this.repository);

  Future<Either<String, List<StoryGroupModel>>> getStories() =>
      repository.getStories();

  Future<Either<String, String>> uploadPhoto(File file) =>
      repository.uploadPhoto(file);

  Future<Either<String, StoryModel>> createStory(String photoUrl) =>
      repository.createStory(photoUrl);

  Future<Either<String, Unit>> deleteStory(int storyId) =>
      repository.deleteStory(storyId);
}
```

- [ ] **Step 4: Написать состояние кубита**

Создать `lib/features/stories/presentation/cubit/stories_state.dart`:

```dart
part of 'stories_cubit.dart';

abstract class StoriesState extends Equatable {
  const StoriesState();

  @override
  List<Object?> get props => [];
}

class StoriesInitial extends StoriesState {}

class StoriesLoading extends StoriesState {}

class StoriesLoaded extends StoriesState {
  final List<StoryGroupModel> groups;
  final Set<int> seenStoryIds;

  const StoriesLoaded({required this.groups, required this.seenStoryIds});

  @override
  List<Object?> get props => [groups, seenStoryIds];
}

class StoriesError extends StoriesState {
  final String message;

  const StoriesError({required this.message});

  @override
  List<Object?> get props => [message];
}
```

- [ ] **Step 5: Написать кубит**

Создать `lib/features/stories/presentation/cubit/stories_cubit.dart`:

```dart
import 'dart:io';

import 'package:equatable/equatable.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:jaidem/features/stories/data/datasources/seen_stories_store.dart';
import 'package:jaidem/features/stories/data/models/story_model.dart';
import 'package:jaidem/features/stories/domain/story_ordering.dart';
import 'package:jaidem/features/stories/domain/usecases/stories_usecase.dart';

part 'stories_state.dart';

class StoriesCubit extends Cubit<StoriesState> {
  final StoriesUsecase storiesUsecase;
  final SeenStoriesStore seenStore;

  StoriesCubit({required this.storiesUsecase, required this.seenStore})
      : super(StoriesInitial());

  int? currentUserId;

  /// Загружает ленту. Ошибку не выбрасываем наверх баннером: сторисы —
  /// второстепенный контент и не должны ломать страницу Жайдемчилер.
  Future<void> fetchFeed() async {
    if (state is! StoriesLoaded) emit(StoriesLoading());

    final result = await storiesUsecase.getStories();

    await result.fold(
      (failure) async => emit(StoriesError(message: failure)),
      (groups) async {
        final activeIds = groups
            .expand((g) => g.stories)
            .map((s) => s.id)
            .toSet();
        await seenStore.pruneTo(activeIds);
        final seen = seenStore.read();

        emit(StoriesLoaded(
          groups: sortStoryGroups(
            groups: groups,
            seenStoryIds: seen,
            currentUserId: currentUserId,
          ),
          seenStoryIds: seen,
        ));
      },
    );
  }

  /// Загружает фото и создаёт сторис. Возвращает true при успехе.
  Future<bool> createStory(File file) async {
    final upload = await storiesUsecase.uploadPhoto(file);
    final url = upload.fold((_) => null, (value) => value);
    if (url == null) return false;

    final created = await storiesUsecase.createStory(url);
    final ok = created.isRight();
    if (ok) await fetchFeed();
    return ok;
  }

  Future<bool> deleteStory(int storyId) async {
    final result = await storiesUsecase.deleteStory(storyId);
    final ok = result.isRight();
    if (ok) await fetchFeed();
    return ok;
  }

  Future<void> markSeen(int storyId) async {
    await seenStore.markSeen(storyId);
    final current = state;
    if (current is StoriesLoaded) {
      emit(StoriesLoaded(
        groups: current.groups,
        seenStoryIds: seenStore.read(),
      ));
    }
  }
}
```

- [ ] **Step 6: Написать файл инъекций фичи**

Создать `lib/features/stories/stories_injection.dart` (по образцу `jaidem_injection.dart`):

```dart
import 'package:jaidem/core/data/injection.dart';
import 'package:jaidem/features/stories/data/datasources/seen_stories_store.dart';
import 'package:jaidem/features/stories/data/datasources/stories_remote_data_source.dart';
import 'package:jaidem/features/stories/data/datasources/stories_remote_data_source_impl.dart';
import 'package:jaidem/features/stories/domain/usecases/stories_usecase.dart';
import 'package:jaidem/features/stories/presentation/cubit/stories_cubit.dart';
import 'package:shared_preferences/shared_preferences.dart';

void storiesInjection() {
  // Data Source
  sl.registerSingleton<StoriesRemoteDataSource>(
      StoriesRemoteDataSourceImpl(dio: sl()));

  sl.registerLazySingleton<SeenStoriesStore>(
      () => SeenStoriesStore(sl<SharedPreferences>()));

  // Usecases
  sl.registerFactory(() => StoriesUsecase(sl()));

  // Cubits
  sl.registerFactory(() => StoriesCubit(storiesUsecase: sl(), seenStore: sl()));
}
```

- [ ] **Step 7: Зарегистрировать фичу в общем контейнере**

В `lib/core/data/injection.dart` добавить импорт:

```dart
import 'package:jaidem/features/stories/stories_injection.dart';
```

и вызов внутри `initInjections()`, после `jaidemInjection();`:

```dart
  storiesInjection();
```

- [ ] **Step 8: Добавить кубит в MultiBlocProvider**

В `lib/core/app/app.dart` добавить импорт:

```dart
import 'package:jaidem/features/stories/presentation/cubit/stories_cubit.dart';
```

и провайдер в список `MultiBlocProvider`, сразу после `BlocProvider<JaidemsCubit>`:

```dart
        BlocProvider<StoriesCubit>(
          create: (context) => sl<StoriesCubit>(),
        ),
```

- [ ] **Step 9: Проверить анализатором**

Run: `flutter analyze lib/features/stories lib/core/data/injection.dart lib/core/app/app.dart`
Expected: `No issues found!`

- [ ] **Step 10: Коммит**

```bash
cd /Users/kyrgyzbala/projects/jaidem/jaidem_student
git add lib/features/stories lib/core/data/injection.dart lib/core/app/app.dart
git commit -m "feat(stories): датасорс, usecase, кубит и регистрация в DI"
```

---

### Task 7: Приложение — полноэкранный просмотрщик и роут

**Files:**
- Create: `jaidem_student/lib/features/stories/presentation/pages/story_viewer_page.dart`
- Modify: `jaidem_student/lib/core/routes/app_router.dart`

**Interfaces:**
- Consumes: `StoryGroupModel` (Task 4), `StoriesCubit` (Task 6), ключи локализации (Task 3)
- Produces: `StoryViewerPage({required List<StoryGroupModel> groups, required int initialGroupIndex, required int? currentUserId})` и сгенерированный `StoryViewerRoute`

- [ ] **Step 1: Написать страницу просмотрщика**

Создать `lib/features/stories/presentation/pages/story_viewer_page.dart`:

```dart
import 'package:auto_route/auto_route.dart';
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:jaidem/core/localization/app_localizations.dart';
import 'package:jaidem/core/utils/style/app_colors.dart';
import 'package:jaidem/features/stories/data/models/story_model.dart';
import 'package:jaidem/features/stories/presentation/cubit/stories_cubit.dart';

const Duration kStoryDuration = Duration(seconds: 5);

@RoutePage()
class StoryViewerPage extends StatefulWidget {
  final List<StoryGroupModel> groups;
  final int initialGroupIndex;
  final int? currentUserId;

  const StoryViewerPage({
    super.key,
    required this.groups,
    required this.initialGroupIndex,
    required this.currentUserId,
  });

  @override
  State<StoryViewerPage> createState() => _StoryViewerPageState();
}

class _StoryViewerPageState extends State<StoryViewerPage>
    with SingleTickerProviderStateMixin {
  late final AnimationController _controller;
  late int _groupIndex;
  int _storyIndex = 0;

  StoryGroupModel get _group => widget.groups[_groupIndex];
  StoryModel get _story => _group.stories[_storyIndex];

  @override
  void initState() {
    super.initState();
    _groupIndex = widget.initialGroupIndex;
    _controller = AnimationController(vsync: this, duration: kStoryDuration)
      ..addStatusListener((status) {
        if (status == AnimationStatus.completed) _next();
      });
    _start();
  }

  void _start() {
    context.read<StoriesCubit>().markSeen(_story.id);
    _controller
      ..reset()
      ..forward();
  }

  void _next() {
    if (_storyIndex < _group.stories.length - 1) {
      setState(() => _storyIndex++);
      _start();
    } else if (_groupIndex < widget.groups.length - 1) {
      setState(() {
        _groupIndex++;
        _storyIndex = 0;
      });
      _start();
    } else {
      context.router.pop();
    }
  }

  void _previous() {
    if (_storyIndex > 0) {
      setState(() => _storyIndex--);
      _start();
    } else if (_groupIndex > 0) {
      setState(() {
        _groupIndex--;
        _storyIndex = _group.stories.length - 1;
      });
      _start();
    }
  }

  Future<void> _confirmDelete() async {
    _controller.stop();
    final confirmed = await showDialog<bool>(
      context: context,
      builder: (dialogContext) => AlertDialog(
        content: Text(context.tr('stories_delete_confirm')),
        actions: [
          TextButton(
            onPressed: () => Navigator.of(dialogContext).pop(false),
            child: Text(context.tr('cancel')),
          ),
          TextButton(
            onPressed: () => Navigator.of(dialogContext).pop(true),
            child: Text(context.tr('stories_delete')),
          ),
        ],
      ),
    );

    if (confirmed != true) {
      _controller.forward();
      return;
    }

    final ok = await context.read<StoriesCubit>().deleteStory(_story.id);
    if (!mounted) return;
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(
        content: Text(context.tr(ok ? 'stories_deleted' : 'error')),
      ),
    );
    if (mounted) context.router.pop();
  }

  @override
  void dispose() {
    _controller.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    final isOwn = widget.currentUserId != null &&
        _group.author.id == widget.currentUserId;

    return Scaffold(
      backgroundColor: Colors.black,
      body: GestureDetector(
        onTapUp: (details) {
          final width = MediaQuery.of(context).size.width;
          if (details.globalPosition.dx < width / 3) {
            _previous();
          } else {
            _next();
          }
        },
        onLongPressStart: (_) => _controller.stop(),
        onLongPressEnd: (_) => _controller.forward(),
        onVerticalDragEnd: (details) {
          if ((details.primaryVelocity ?? 0) > 200) context.router.pop();
        },
        child: Stack(
          children: [
            Positioned.fill(
              child: Image.network(
                _story.photo,
                fit: BoxFit.contain,
                errorBuilder: (_, __, ___) => const Center(
                  child: Icon(Icons.broken_image_outlined,
                      color: Colors.white54, size: 48),
                ),
              ),
            ),
            SafeArea(
              child: Column(
                children: [
                  _buildProgressBars(),
                  _buildHeader(isOwn),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }

  Widget _buildProgressBars() {
    return Padding(
      padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 8),
      child: Row(
        children: List.generate(_group.stories.length, (index) {
          return Expanded(
            child: Container(
              height: 2.5,
              margin: const EdgeInsets.symmetric(horizontal: 2),
              decoration: BoxDecoration(
                color: Colors.white24,
                borderRadius: BorderRadius.circular(2),
              ),
              child: AnimatedBuilder(
                animation: _controller,
                builder: (context, _) {
                  final double value = index < _storyIndex
                      ? 1
                      : index == _storyIndex
                          ? _controller.value
                          : 0;
                  return FractionallySizedBox(
                    alignment: Alignment.centerLeft,
                    widthFactor: value,
                    child: Container(
                      decoration: BoxDecoration(
                        color: Colors.white,
                        borderRadius: BorderRadius.circular(2),
                      ),
                    ),
                  );
                },
              ),
            ),
          );
        }),
      ),
    );
  }

  Widget _buildHeader(bool isOwn) {
    return Padding(
      padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 4),
      child: Row(
        children: [
          CircleAvatar(
            radius: 18,
            backgroundColor: AppColors.primary.shade200,
            backgroundImage: (_group.author.avatar != null &&
                    _group.author.avatar!.isNotEmpty)
                ? NetworkImage(_group.author.avatar!)
                : null,
            child: (_group.author.avatar == null ||
                    _group.author.avatar!.isEmpty)
                ? const Icon(Icons.person, color: Colors.white, size: 18)
                : null,
          ),
          const SizedBox(width: 10),
          Expanded(
            child: Text(
              _group.author.fullname ?? '',
              style: const TextStyle(
                color: Colors.white,
                fontWeight: FontWeight.w600,
                fontSize: 14,
              ),
              overflow: TextOverflow.ellipsis,
            ),
          ),
          if (isOwn)
            IconButton(
              onPressed: _confirmDelete,
              icon: const Icon(Icons.delete_outline, color: Colors.white),
            ),
          IconButton(
            onPressed: () => context.router.pop(),
            icon: const Icon(Icons.close_rounded, color: Colors.white),
          ),
        ],
      ),
    );
  }
}
```

- [ ] **Step 2: Проверить, что ключи `cancel` и `error` существуют**

Run: `grep -nE "'cancel'|'error'" lib/core/localization/app_localizations.dart | head`
Expected: по три вхождения каждого (по одному на язык).

Если какого-то ключа нет — добавить его во все три мапы (`cancel`: `Жокко чыгаруу` / `Отмена` / `Cancel`; `error`: `Ката` / `Ошибка` / `Error`).

- [ ] **Step 3: Зарегистрировать роут**

В `lib/core/routes/app_router.dart` добавить импорт:

```dart
import 'package:jaidem/features/stories/presentation/pages/story_viewer_page.dart';
```

и роут в список `routes`, после блока с `JaidemDetailRoute`:

```dart
        CustomRoute(
          page: StoryViewerRoute.page,
          path: '/stories',
          transitionsBuilder: TransitionsBuilders.fadeIn,
          duration: const Duration(milliseconds: 300),
        ),
```

- [ ] **Step 4: Прогнать кодогенерацию**

Run: `dart run build_runner build --delete-conflicting-outputs`
Expected: `Succeeded after ...` и обновлённый `lib/core/routes/app_router.gr.dart`, содержащий `StoryViewerRoute`.

- [ ] **Step 5: Проверить анализатором**

Run: `flutter analyze lib/features/stories lib/core/routes/app_router.dart`
Expected: `No issues found!`

- [ ] **Step 6: Коммит**

```bash
cd /Users/kyrgyzbala/projects/jaidem/jaidem_student
git add lib/features/stories/presentation/pages/story_viewer_page.dart lib/core/routes/app_router.dart lib/core/routes/app_router.gr.dart
git commit -m "feat(stories): полноэкранный просмотрщик сторисов и роут"
```

---

### Task 8: Приложение — лента аватарок и монтаж на странице Жайдемчилер

**Files:**
- Create: `jaidem_student/lib/features/stories/presentation/widgets/stories_strip.dart`
- Modify: `jaidem_student/lib/features/jaidems/presentation/pages/jaidems_page.dart`

**Interfaces:**
- Consumes: `StoriesCubit` (Task 6), `StoryViewerRoute` (Task 7), ключи локализации (Task 3)
- Produces: виджет `StoriesStrip()` без обязательных параметров — сам берёт состояние из `StoriesCubit`

- [ ] **Step 1: Написать виджет ленты**

Создать `lib/features/stories/presentation/widgets/stories_strip.dart`:

```dart
import 'dart:io';

import 'package:auto_route/auto_route.dart';
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:image_picker/image_picker.dart';
import 'package:jaidem/core/data/injection.dart';
import 'package:jaidem/core/localization/app_localizations.dart';
import 'package:jaidem/core/routes/app_router.dart';
import 'package:jaidem/core/utils/constants/app_constants.dart';
import 'package:jaidem/core/utils/style/app_colors.dart';
import 'package:jaidem/features/stories/data/models/story_model.dart';
import 'package:jaidem/features/stories/presentation/cubit/stories_cubit.dart';
import 'package:jwt_decoder/jwt_decoder.dart';
import 'package:shared_preferences/shared_preferences.dart';

class StoriesStrip extends StatefulWidget {
  const StoriesStrip({super.key});

  @override
  State<StoriesStrip> createState() => _StoriesStripState();
}

class _StoriesStripState extends State<StoriesStrip> {
  bool _isUploading = false;

  @override
  void initState() {
    super.initState();
    final cubit = context.read<StoriesCubit>();
    cubit.currentUserId = _readCurrentUserId();
    cubit.fetchFeed();
  }

  int? _readCurrentUserId() {
    final token = sl<SharedPreferences>().getString(AppConstants.accessToken);
    if (token == null || token.isEmpty) return null;
    try {
      final claims = JwtDecoder.decode(token);
      final raw = claims['user_id'];
      return raw is int ? raw : int.tryParse('$raw');
    } catch (_) {
      return null;
    }
  }

  Future<void> _addStory() async {
    final picked = await ImagePicker().pickImage(
      source: ImageSource.gallery,
      maxWidth: 1080,
      imageQuality: 85,
    );
    if (picked == null || !mounted) return;

    setState(() => _isUploading = true);
    final ok = await context.read<StoriesCubit>().createStory(File(picked.path));
    if (!mounted) return;
    setState(() => _isUploading = false);

    if (!ok) {
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(content: Text(context.tr('stories_create_failed'))),
      );
    }
  }

  void _openViewer(List<StoryGroupModel> groups, int index) {
    context.router.push(
      StoryViewerRoute(
        groups: groups,
        initialGroupIndex: index,
        currentUserId: context.read<StoriesCubit>().currentUserId,
      ),
    );
  }

  @override
  Widget build(BuildContext context) {
    return BlocBuilder<StoriesCubit, StoriesState>(
      builder: (context, state) {
        // Ошибку и загрузку не показываем: сторисы второстепенны и не должны
        // мешать списку участников. В худшем случае видно только «Твоя история».
        final groups = state is StoriesLoaded
            ? state.groups
            : const <StoryGroupModel>[];
        final seen = state is StoriesLoaded ? state.seenStoryIds : const <int>{};

        return Container(
          color: Colors.white,
          padding: const EdgeInsets.symmetric(vertical: 12),
          child: SizedBox(
            height: 96,
            child: ListView.separated(
              scrollDirection: Axis.horizontal,
              padding: const EdgeInsets.symmetric(horizontal: 12),
              itemCount: groups.length + 1,
              separatorBuilder: (_, __) => const SizedBox(width: 14),
              itemBuilder: (context, index) {
                if (index == 0) return _buildAddTile();
                final group = groups[index - 1];
                final isSeen =
                    group.stories.every((s) => seen.contains(s.id));
                return _buildAuthorTile(
                  group: group,
                  isSeen: isSeen,
                  onTap: () => _openViewer(groups, index - 1),
                );
              },
            ),
          ),
        );
      },
    );
  }

  Widget _buildAddTile() {
    return GestureDetector(
      onTap: _isUploading ? null : _addStory,
      child: SizedBox(
        width: 68,
        child: Column(
          children: [
            Container(
              width: 62,
              height: 62,
              decoration: BoxDecoration(
                shape: BoxShape.circle,
                color: AppColors.primary.withValues(alpha: 0.08),
                border: Border.all(color: AppColors.primary, width: 1.5),
              ),
              child: _isUploading
                  ? Padding(
                      padding: const EdgeInsets.all(20),
                      child: CircularProgressIndicator(
                        strokeWidth: 2,
                        valueColor:
                            AlwaysStoppedAnimation<Color>(AppColors.primary),
                      ),
                    )
                  : Icon(Icons.add_rounded,
                      color: AppColors.primary, size: 28),
            ),
            const SizedBox(height: 6),
            Text(
              context.tr('stories_your_story'),
              maxLines: 1,
              overflow: TextOverflow.ellipsis,
              style: const TextStyle(fontSize: 11, fontWeight: FontWeight.w500),
            ),
          ],
        ),
      ),
    );
  }

  Widget _buildAuthorTile({
    required StoryGroupModel group,
    required bool isSeen,
    required VoidCallback onTap,
  }) {
    return GestureDetector(
      onTap: onTap,
      child: SizedBox(
        width: 68,
        child: Column(
          children: [
            Container(
              width: 62,
              height: 62,
              padding: const EdgeInsets.all(2.5),
              decoration: BoxDecoration(
                shape: BoxShape.circle,
                gradient: isSeen
                    ? null
                    : LinearGradient(
                        colors: [AppColors.primary, AppColors.secondary],
                        begin: Alignment.topLeft,
                        end: Alignment.bottomRight,
                      ),
                border: isSeen
                    ? Border.all(color: Colors.grey.shade300, width: 2)
                    : null,
              ),
              child: Container(
                padding: const EdgeInsets.all(2),
                decoration: const BoxDecoration(
                  shape: BoxShape.circle,
                  color: Colors.white,
                ),
                child: CircleAvatar(
                  backgroundColor: AppColors.primary.shade100,
                  backgroundImage:
                      (group.author.avatar != null && group.author.avatar!.isNotEmpty)
                          ? NetworkImage(group.author.avatar!)
                          : null,
                  child: (group.author.avatar == null ||
                          group.author.avatar!.isEmpty)
                      ? const Icon(Icons.person, color: Colors.white, size: 22)
                      : null,
                ),
              ),
            ),
            const SizedBox(height: 6),
            Text(
              group.author.fullname ?? '',
              maxLines: 1,
              overflow: TextOverflow.ellipsis,
              style: const TextStyle(fontSize: 11),
            ),
          ],
        ),
      ),
    );
  }
}
```

- [ ] **Step 2: Проверить имя ключа токена**

Run: `grep -n "accessToken" lib/core/utils/constants/app_constants.dart`
Expected: строка вида `static const String accessToken = '...';`

Если константа названа иначе — поправить импорт и обращение в `_readCurrentUserId`.

- [ ] **Step 3: Смонтировать ленту в шапке страницы Жайдемчилер**

В `lib/features/jaidems/presentation/pages/jaidems_page.dart` добавить импорт:

```dart
import 'package:jaidem/features/stories/presentation/widgets/stories_strip.dart';
```

и в `headerSliverBuilder` добавить ленту после `_buildSliverAppBar`:

```dart
          headerSliverBuilder: (context, innerBoxIsScrolled) {
            return [
              _buildSliverAppBar(innerBoxIsScrolled),
              const SliverToBoxAdapter(child: StoriesStrip()),
            ];
          },
```

Лента живёт именно в шапке, а не в теле: тело перерисовывается по состояниям
`JaidemsLoading / JaidemsError / empty`, и внутри него лента пропадала бы при
ошибке загрузки участников и дублировалась бы в трёх ветках билдера.

- [ ] **Step 4: Проверить анализатором весь проект**

Run: `flutter analyze`
Expected: в изменённых файлах замечаний нет.

Проверить точечно:

Run: `flutter analyze 2>&1 | grep -E "stories|jaidems_page" || echo "CLEAN"`
Expected: `CLEAN`

- [ ] **Step 5: Прогнать все тесты**

Run: `flutter test`
Expected: `All tests passed!`

- [ ] **Step 6: Ручная проверка на устройстве**

Run: `flutter run`

Проверить по списку:
1. На странице Жайдемчилер под шапкой видна лента с кнопкой «Твоя история».
2. Тап по «Твоя история» → выбор фото → после загрузки свой сторис появляется первым.
3. Тап по чужому аватару → полноэкранный просмотр, полоски прогресса заполняются, через ~5 сек переход к следующему.
4. Тап справа/слева — вперёд/назад; долгое нажатие — пауза; свайп вниз — закрытие.
5. После просмотра кольцо автора становится серым.
6. На своём сторисе есть иконка удаления → подтверждение → сторис исчезает из ленты.
7. Выключить интернет и открыть страницу: список участников показывает ошибку, а лента просто пустая — страница не падает.

- [ ] **Step 7: Коммит**

```bash
cd /Users/kyrgyzbala/projects/jaidem/jaidem_student
git add lib/features/stories/presentation/widgets/stories_strip.dart lib/features/jaidems/presentation/pages/jaidems_page.dart
git commit -m "feat(stories): лента сторисов на странице Жайдемчилер"
```

---

## Проверка покрытия спека

| Требование спека | Задача |
|---|---|
| Модель `Story` (author CASCADE, photo URL, created_at) | Task 1 |
| `STORY_TTL_HOURS = 24` в одном месте | Task 1 |
| Миграция | Task 1 |
| Модерация через Django-админку | Task 1 |
| `GET /core/story/` сгруппировано по автору | Task 2 |
| `pagination_class = None` | Task 2 |
| Автор из `request.user`, не из тела | Task 2 |
| `DELETE` только своего | Task 2 |
| `IsAuthenticated` | Task 2 |
| 3 бэкенд-теста | Task 2 |
| Переиспользование `/category/image/` | Task 3, 6 |
| Вынос захардкоженного URL | Task 3 |
| Локализация × 3 языка | Task 3 |
| Модели сториса | Task 4 |
| `SeenStoriesStore` + самоочистка | Task 5 |
| Сортировка свои → непросмотренные → просмотренные | Task 5 |
| Юнит-тесты чистой логики | Task 4, 5 |
| Датасорс / usecase / кубит / DI | Task 6 |
| Просмотрщик: прогресс, тапы, пауза, свайп, удаление | Task 7 |
| Роут + кодогенерация auto_route | Task 7 |
| Лента аватарок, кольца, «Твоя история» | Task 8 |
| Монтаж в `headerSliverBuilder` | Task 8 |
| Сжатие фото `maxWidth: 1080, imageQuality: 85` | Task 8 |
| Ошибка ленты не ломает страницу | Task 6 (кубит), Task 8 (виджет) |

Вне объёма v1 (видео, реакции, ответы, highlights, «кто посмотрел», жалобы, синхронизация просмотров, пуши, изменения в админ-приложениях) задач не имеет — это ожидаемо.
